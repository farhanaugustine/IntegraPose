# Splash Assets

Place an animated GIF named `splash.gif` in this folder to show it during startup.

IntegraPose will fall back to the progress bar-only splash if the GIF is missing or cannot be loaded.

