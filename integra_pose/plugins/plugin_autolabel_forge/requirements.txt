# Optional dependency add-ons for AutoLabel Forge.
#
# Install after the main GUI environment:
#   pip install -r requirements.txt
#   pip install -r integra_pose/plugins/plugin_autolabel_forge/requirements.txt
#
# The main package already provides NumPy, GUI-enabled OpenCV, PyYAML,
# Supervision, tqdm, and Ultralytics. Do not add packages that force the
# headless OpenCV wheel into this desktop environment.
autodistill==0.1.29
autodistill-grounding-dino==0.1.4
transformers==4.57.6
