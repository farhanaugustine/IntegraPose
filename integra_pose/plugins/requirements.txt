# Optional plugin dependencies for IntegraPose.
#
# Recommended:
#   pip install "IntegraPose[plugins]"
#
# Or, if you already installed the core GUI:
#   pip install -r integra_pose/plugins/requirements.txt
#
# Note: PyTorch is still installed separately (CPU/CUDA wheel per https://pytorch.org/get-started/locally/).
# Some plugins also have their own add-on requirement files:
#   - integra_pose/plugins/plugin_behavior_scope/requirements.txt
#   - integra_pose/plugins/plugin_autolabel_forge/requirements.txt
#   - integra_pose/plugins/plugin_fura_imaging_lab/requirements.txt
#   - requirements-albumentations-gui.txt (Dataset Augmentor Lab)

PySide6>=6.5.0
scipy==1.11.4
seaborn==0.13.2
statsmodels==0.14.2
scikit-learn==1.3.1
umap-learn==0.5.7
hdbscan==0.8.40
hmmlearn==0.3.0
networkx==3.3
jsonschema==4.21.1
plotly==5.24.1
imbalanced-learn==0.11.0
pyEDM==2.2.3
autodistill==0.1.29
autodistill-grounding-dino==0.1.4
roboflow==1.2.13
# Pinned because roboflow 1.2.13 hard-pins idna==3.7. Without this line,
# pip's resolver may install a newer idna and then warn about a conflict.
# idna 3.7 is API-compatible with everything else in the stack.
idna==3.7
transformers==4.57.6

# Optional YOLO-NAS pipeline (requires Python <=3.10 and NumPy <=1.23.x; use a separate env).
# super-gradients==3.7.1
